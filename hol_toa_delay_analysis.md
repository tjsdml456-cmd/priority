## HOL_TOA 및 큐잉 딜레이 동작 정리

이 문서는 srsRAN 코드에서 **HOL_TOA 기반 큐잉 딜레이가 0ms로 떨어지는 현상**과,  
그 원인이 되는 두 가지 메커니즘(**hol_toa min clamp**, **slot wrap-around clamp**)의 동작을 정리한 것이다.

---

### 1. HOL_TOA Min Clamp

- **위치**: `ue_event_manager.cpp:85`
- **코드 동작(개념)**:
  - RLC 계층에서 `steady_clock` 기준으로 기록된 `hol_toa`를 MAC 계층의 `slot_point`로 변환하는 과정에서,
    변환 결과가 **현재 슬롯보다 미래를 가리키는 경우**가 발생할 수 있다.
  - 이를 방지하기 위해 다음과 같이 **현재 슬롯(`sl`)을 상한으로 하는 min clamp**를 적용한다:

    ```cpp
    dl_bo.hol_toa = std::min(sl, slot_point{sl.numerology(), (unsigned)hol_toa});
    ```

- **도입 배경 / 원인**:
  - **타임스탬프 보정 오류**  
    - RLC는 `steady_clock`, MAC/스케줄러는 `system_clock` 혹은 슬롯 인덱스를 사용한다.
    - 이 두 기준 간 변환 과정에서 상수 오프셋/스케일/반올림 차이로 오차가 발생할 수 있다.
  - **시간 동기화 문제**  
    - 서로 다른 클록 도메인(steady vs system)이 완벽히 동기화되어 있지 않으면,
      변환된 시각이 실제보다 앞/뒤로 밀릴 수 있다.
  - **Slot 번호 계산 오류**  
    - 시간 → 슬롯 번호 변환 시, 나눗셈 / 반올림 / 버림 처리에서 미세한 오차가 생겨,
      변환된 `hol_toa`가 현재 슬롯보다 약간 더 큰 값이 되는 경우가 있다.

- **결과**:
  - 변환 결과 `hol_toa > sl` 이면, min clamp에 의해 **`hol_toa = sl`** 로 덮어쓰기 된다.
  - 이후 스케줄러에서 큐잉 딜레이를 계산할 때:

    ```cpp
    hol_delay_ms = (slot_tx - hol_toa) / slot_tx.nof_slots_per_subframe();
    ```

    - 현재 슬롯(`slot_tx`)과 `hol_toa`가 동일하게 되므로, **`hol_delay_ms = 0ms`** 로 계산된다.
  - 즉, 실제로는 딜레이가 존재해도, **보호 로직(min clamp) 때문에 측정값이 0ms로 리셋**되는 효과가 나타날 수 있다.

---

### 2. Slot Wrap-Around Clamp

- **위치**: `srsran_scheduler_adapter.cpp:231`
- **코드 동작(개념)**:

  ```cpp
  static constexpr microseconds half_system_frame_dur = milliseconds{10240 / 2}; // 5120ms
  hol_delay = std::min(std::max(hol_delay, microseconds{0}), half_system_frame_dur);
  ```

  - 시스템 프레임 주기는 **10.24초 (10240ms)** 로 가정한다.
  - 슬롯 번호는 이 프레임 내에서 **순환(wrap-around)** 하므로,
    두 시각의 차이가 프레임의 절반인 **5.12초(5120ms)** 를 넘으면
    "어느 쪽이 과거/미래인지"가 모호해진다.
  - 이를 막기 위해, 큐잉 딜레이(`hol_delay`)를 **\[0ms, 5120ms\] 범위로 clamp** 한다.

- **도입 배경 / 원인**:
  - **시스템 프레임 주기 10.24초**  
    - 슬롯 인덱스가 프레임 내에서 순환하기 때문에, 너무 큰 시간 차이를 그대로 슬롯 차이로 해석하면
      wrap-around로 인해 잘못된(음수/극단적인) 딜레이가 나올 수 있다.
  - **wrap-around 모호성 방지**  
    - 두 시각 간 차이가 half-frame(5.12초)을 넘으면,
      "과거"와 "미래" 해석이 뒤바뀔 수 있으므로, 안전하게 **최대 5120ms로 제한**한다.

- **결과**:
  - 실제 큐잉 딜레이가 6~8초처럼 길어져도,
    로직 상에서는 **최대 5120ms까지만 표현**되며, 그 이상은 **5120ms로 포화(saturation)** 된다.
  - 그래프 상으로는 **대략 5000ms 근처에서 더 이상 증가하지 않고 평탄해지는 구간**으로 보인다.

---

### 3. 두 메커니즘의 상호작용 및 그래프 패턴

두 보호 메커니즘이 **클록 변환 오차**와 결합되면, 다음과 같은 흐름이 발생할 수 있다:

1. **큐잉 딜레이 증가**
   - 패킷이 계속 쌓이면서 실제 큐잉 딜레이가 증가한다.
2. **Slot wrap-around clamp 적용**
   - 계산된 `hol_delay`가 5120ms를 넘으면,  
     `hol_delay = 5120ms` 로 clamp 된다.
3. **클록 변환 오차 발생**
   - RLC의 `steady_clock` 기준 `hol_toa`를 MAC/스케줄러의 슬롯 기준으로 변환하는 과정에서
     미세한 오차가 누적된다.
   - 이 오차로 인해, 변환된 `hol_toa`가 **현재 슬롯보다 미래(sl < hol_toa)** 로 보이는 순간이 온다.
4. **HOL_TOA Min Clamp 작동**
   - `dl_bo.hol_toa = std::min(sl, slot_point{...})` 로 인해,
     `hol_toa`가 **강제로 현재 슬롯(`sl`)로 덮어쓰기** 된다.
5. **큐잉 딜레이 0ms로 계산**
   - 스케줄러 쪽에서 `hol_delay_ms = (slot_tx - hol_toa)` 를 계산하면,
     `slot_tx`와 `hol_toa`가 동일하게 되어 **`hol_delay_ms = 0ms`** 로 계산된다.

이 과정을 통해, 그래프 상에서 다음과 같은 **특징적인 패턴**이 나타난다:

- 시작: **0ms** 근처에서 시작
- 이후: 트래픽이 쌓이면서 **점진적으로 증가**
- 중간: **5000ms 근처에서 최고점**(slot wrap-around clamp에 의한 포화)
- 어느 시점: 클록 변환 오차 + hol_toa min clamp가 겹쳐서  
  **큐잉 딜레이가 갑자기 0ms로 떨어짐**
- 이후에도 Throughput은 계속 나오지만,  
  **큐잉 딜레이 측정값은 0ms로 리셋된 상태에서 다시 증가를 시작**하는 패턴이 반복될 수 있다.

---

### 4. 해석 및 시사점

- 이 두 메커니즘은 모두 **타임스탬프/슬롯 이상값을 막기 위한 방어 로직**이다.
- 그러나, 특히 **장시간 큐잉 딜레이를 관찰**할 때는 다음과 같은 부작용이 있다:
  - 실제 큐잉 딜레이가 매우 길어져도,  
    **5120ms 이상은 표현되지 않고 포화**된다.
  - 클록 변환 오차가 누적되어 hol_toa min clamp가 발동하면,  
    **측정된 딜레이가 갑자기 0ms로 리셋**된다.
- 따라서,
  - **"Throughput은 계속 나오는데 Delay 그래프만 0으로 급락"** 하는 현상은
    스케줄러/큐의 실제 동작 문제라기보다는,
    **측정·변환 계층에서의 보호 로직 + 클록 불일치로 인한 관측상의 artifact**로 보는 것이 타당하다.

---

### 5. 개선 아이디어(고려 사항)

아래는 동작 변경을 고려할 때 참고할 수 있는 방향성이다 (현재 코드에는 적용되지 않은 제안):

- **단일 기준 클록으로 통일**
  - RLC와 MAC/스케줄러가 가능한 한 동일한 시간 기준(예: 모두 `system_clock` 또는 공통 slot counter)을 사용하도록 설계해,
    steady ↔ system 변환을 최소화하거나 명확히 정의한다.

- **hol_toa min clamp 완화**
  - `hol_toa`를 현재 슬롯로 강제로 덮어쓰는 대신,
    - 예: `sl - ε` (아주 작은 margin) 쪽으로 이동시키거나,
    - 미래 슬롯으로 튄 경우를 별도 카운터/로그로만 기록하고, 딜레이 계산에는 이전 유효값을 사용하는 등
      **관측값 리셋을 줄이는 방식**을 검토할 수 있다.

- **wrap-around 범위/정책 재검토**
  - 실제 운영 환경에서 큐잉 딜레이가 5초 이상 누적될 수 있는 시나리오가 있다면,
    half-frame 가정(5120ms)이나 clamp 정책을 조정하거나,
    wrap-around를 명시적으로 다루는 별도 로직을 도입하는 것도 한 방법이다.

이 문서는 위 현상이 **설계된 보호 메커니즘의 상호작용과 클록 변환 오차의 결과**임을 이해하는 데 목적이 있다.

---

### 6. 예시 시나리오: 3 UE iperf3 테스트와 그래프 해석

다음은 실제 테스트 스크립트(iperf3 + 동적 5QI 변경)를 기반으로 한 **3개 UE의 큐잉 딜레이 그래프**를 위 메커니즘으로 해석한 예시이다.

#### 6.1 테스트 시나리오 개요

- **트래픽 패턴(요약)**:
  - UE0:
    - 0–20초: 초기 5QI(예: 9)로 DL/UL 트래픽
    - 20–30초: 트래픽 없음 (idle)
    - 30–50초: 5QI=3 (GBR 10 Mbps)로 트래픽
    - 50–60초: idle
    - 60–80초: 5QI=85 (delay-critical GBR 15 Mbps)로 트래픽
  - UE1, UE2:
    - 0–80초: 동일 5QI로 **계속 iperf3** 실행 (중단 없음)

- **그래프 특성(관측)**:
  - 각 UE의 큐잉 딜레이가 **트래픽이 실제로 발생하는 구간에서만 상승**하고,
    idle 구간이나 스케줄링 여유가 있는 구간에서는 **0 또는 0에 가까운 값**으로 유지된다.
  - 세 UE 모두 최대 지연이 **대략 4.5~5.1초** 근처에서 포화되는 모양을 보인다.
  - 특정 시점에서 **4~5초 근처 최고점에서 바로 0ms로 수직 낙하**하는 스파이크가 반복적으로 나타난다.

#### 6.2 wrap-around clamp와 최대 지연 높이

- 세 UE의 큐잉 딜레이가 **대략 5초 부근에서 더 이상 증가하지 않는 이유**는:
  - \[섹션 2\]에서 설명한 대로, `hol_delay`가 **\[0ms, 5120ms\]로 clamp** 되기 때문이다.
  - 실제 큐잉 딜레이가 6~8초까지 길어져도,  
    측정된 값은 항상 **최대 5120ms(≈5.12초)** 를 넘지 않는다.
- 따라서 그래프에서 여러 UE의 최고점이 비슷한 높이(약 5초)에서 잘리는 것은,
  - 스케줄러 설계/환경과 무관하게,  
  - **wrap-around 보호 로직에 의해 상한이 고정되어 있기 때문**으로 해석할 수 있다.

#### 6.3 hol_toa min clamp와 “수직 낙하” 패턴

- 각 UE의 곡선을 보면, 다음과 같은 패턴이 반복된다:
  - 0ms 근처에서 시작 → 수 초 동안 지연이 점점 증가 → 4~5초 부근에서 포화  
  - 어느 순간 **지연값이 갑자기 0ms로 떨어짐** → 이후 다시 0에서부터 증가 시작
- 이 현상은 \[섹션 3\]의 흐름과 정확히 대응한다:
  1. 큐잉 딜레이가 증가하여 `hol_delay ≈ 5120ms` 까지 포화
  2. RLC `steady_clock` → MAC 슬롯으로의 변환 오차가 누적되어  
     변환된 `hol_toa`가 **현재 슬롯보다 미래 슬롯**으로 보이는 시점이 발생
  3. `dl_bo.hol_toa = std::min(sl, slot_point{...})` 로 인해  
     `hol_toa`가 **현재 슬롯로 덮어쓰기** 된다
  4. 그 샘플에서 `hol_delay_ms = (slot_tx - hol_toa)` 가 **0ms**로 계산되어  
     그래프가 수직선처럼 0으로 떨어진다
- 이후에는 새로 생성된 패킷들이 다시 큐에 쌓이면서,
  - **0ms에서부터 지연이 다시 증가**하는 구간이 나타나고,
  - 위 과정이 반복될 때마다 **5초 근처 최고점 ↔ 0ms 사이를 왕복하는 스파이크**들이 생성된다.

#### 6.4 UE별 0ms 근처 “평지” 구간 해석

- 어느 UE의 그래프에서는 **상당히 긴 시간 동안 0 또는 0에 가까운 값**만 찍히는 구간이 존재한다.
- 이 구간은 clamp 버그로 강제로 0이 고정된 것이 아니라, 다음과 같이 해석하는 것이 타당하다:
  - 해당 시점에는
    - iperf3 부하가 상대적으로 약하거나,
    - 5QI/GBR 설정과 스케줄러 자원 분배 덕분에
      **도착한 패킷이 거의 즉시 전송**되는 상태이다.
  - HOL 기준으로 지연이 **수십 ms~수백 ms 수준**에 머물면,
    - y축 스케일(0~5000ms) 상에서는 **그래프가 사실상 0 근처의 “평지”처럼** 보인다.
- 요약하면,
  - **스파이크 꼭대기와 수직 낙하는 측정 아티팩트 영향이 크고**,  
  - **그 사이의 긴 0 근처 구간은 실제로 큐가 거의 비어 있거나 얕게 유지되는 “건강한” 구간**으로 보는 것이 맞다.
